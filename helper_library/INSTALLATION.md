# Package Installation

The python helper library can be installed from a local clone 

```bash
pip3 install setuptools

pip3 install ./helper_library
```

Or install directly frim Github:

```bash
pip3 install "nv_cloud_function_helpers @ git+https://github.com/NVIDIA/nv-cloud-function-helpers.git#subdirectory=helper_library"
```